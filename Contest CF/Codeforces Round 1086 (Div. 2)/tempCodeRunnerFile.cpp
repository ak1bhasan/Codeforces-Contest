
    // for( int i = n - 2; i >= 0; i-- ) {
    //     dl d1 = ( v[i].S * dp[i + 1] ) + v[i].F;
    //     dp[i] = max( d1, dp[i + 1] );
    // }
    // cout << fixed << setprecision(10) << dp[0] << endl;